'use strict';

/**
 * Storage unit Module dependencies
 */
var path = require('path'),
  config = require(path.resolve('./config/config'));

/**
 * Storage unit Module init function.
 */
module.exports = function (app, db) {

};
